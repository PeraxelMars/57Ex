@model IEnumerable<string>

<table class="table">
    <tr>
        <th>
            Results
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @item
        </td>
    </tr>
}

</table>
