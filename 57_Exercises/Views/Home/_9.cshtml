
@using _57_Exercises.Controllers

@model _57_Exercises.ViewModels.Ex9ViewModel

@{
    ViewBag.Title = "9";
}

@using (Ajax.BeginForm(actionName: nameof(HomeController._9), ajaxOptions: new AjaxOptions() { HttpMethod = "POST", UpdateTargetId = "result", InsertionMode = InsertionMode.Replace }))
{
    @Html.AntiForgeryToken()

    @Html.ValidationSummary(false, "", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(m => m.Width, "Width? ")
        @Html.EditorFor(m => m.Width, new { htmlAttributes = new { @class = "form-control", autofocus = "", required = "required" } })
    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Length, "Length? ")
        @Html.EditorFor(m => m.Length, new { htmlAttributes = new { @class = "form-control", required = "required" } })
    </div>
    <button type="submit" id="btnSubmit" class="btn btn-default">Ok</button>
}

<p>
    <div id="result"></div>
    <h2 class="text-primary text-warning" id="error"></h2>
</p>